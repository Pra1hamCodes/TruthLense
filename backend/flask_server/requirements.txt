flask
flask-cors
opencv-python-headless
numpy
h5py
keras>=3.6,<3.10
# Torch is the Keras 3 backend (TensorFlow's native DLLs are blocked by
# Windows Smart App Control on the dev host, so we run Keras on torch).
torch>=2.2
torchvision
